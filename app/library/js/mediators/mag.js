define(
    [
        'jquery'
    ],
    function(
        $
    ) {

        'use strict';

        var init = function init( pt ){

            // do stuff
        };

        return {
            init: init
        };
    }
);
